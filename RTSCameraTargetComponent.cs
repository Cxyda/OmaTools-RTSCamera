using UnityEngine;

namespace Plugins.O.M.A.Games.RTS_Camera
{
    /// <summary>
    /// Attach this component to any GameObject that can be targeted by the RTS camera. When this object gets clicked
    /// the RTS Camera will follow this target until a user input is received.
    /// </summary>
    public class RTSCameraTargetComponent : MonoBehaviour
    {
        // This is just a dummy component to avoid using tags.
    }
}